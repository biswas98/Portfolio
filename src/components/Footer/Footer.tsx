import React from "react";

export const Footer = () => {
  return (
    <div>
      {true ? (
        <a href="https://dogtinder.biswas.live/" target="_blank">
          Click
        </a>
      ) : null}
    </div>
  );
};
